# 开发日志 — 2026-07-10

## 项目启动

**项目名称**：宏观仪表盘 (Macro Dashboard)
**技术栈**：SwiftUI + SwiftData + Swift Charts，macOS 14+
**编译状态**：BUILD SUCCEEDED（28 个文件，0 错误）
**网络状态**：跳过，待恢复后实施 Phase 2

## 已完成

- [x] 需求沟通与整理
- [x] 数据源方案确认（FRED API + Yahoo Finance v8 API）
- [x] 实现计划编写与审批
- [x] 开发日志系统建立

### Phase 1：骨架与数据模型 ✅
- [x] Xcode 项目自动生成（28 个 Swift 文件）
- [x] `IndicatorCategory`、`IndicatorDefinition`、`IndicatorValue`、`IndicatorTimeSeriesPoint`、`DirectionalSentiment`、`RefreshState`
- [x] 新增 `MarketSignal` — 8 种组合信号定义（条件、说明、颜色）
- [x] `StoredIndicatorRecord` + `PersistenceController`

### Phase 2：网络层 ⏳（网络恢复后实施）

### Phase 3：ViewModel 层 ✅
- [x] `IndicatorCardViewModel` — 单指标卡片状态
- [x] `DashboardViewModel` — 主仪表盘状态 + mock数据 + 信号评估
- [x] `IndicatorDetailViewModel` — 详情页图表数据

### Phase 4：仪表盘 UI ✅
- [x] `ValueChangeBadge`、`SparklineView`、`RefreshButton`、`LastUpdatedBar`、`ErrorBanner`
- [x] `IndicatorCardView` — 三态卡片（数据/加载/错误）+ 信息弹出框（涨跌含义）
- [x] `CategoryGroupView`、`DashboardView`
- [x] 移除 Mac 侧栏，改为全屏仪表盘布局（iOS 可保留侧栏）

### Phase 5：详情页与图表 ✅
- [x] `IndicatorChartView`、`IndicatorStatsBar`、`IndicatorDetailView`
- [x] `SettingsView`
- [x] 详情页关闭方式：点击外部遮罩 / X 按钮 / Escape 键

### Phase 6：打磨 ✅
- [x] 加载骨架屏、错误状态卡片、数据过期提示、暗色模式

### 新增：组合判断市场信号 ✅
- [x] `MarketSignal` — 8 种组合信号模型
- [x] `SignalEngine` — 信号评估引擎（全部条件满足才触发）
- [x] `SignalOverviewBar` — 仪表盘顶部信号概览栏
  - 活跃信号显示彩色胶囊，不活跃信号灰色
  - 点击胶囊显示信号说明
  - 以当日涨跌方向为判断依据

## 待办

### Phase 2：网络层（网络恢复后实施）
- [ ] DataSourceProtocol、FREDService、YahooFinanceService
- [ ] RateLimiter、RatioComputationService、IndicatorFetchCoordinator
- [ ] 替换 mock 数据为真实数据

## Mock 数据下的信号示例

当前 mock 数据显示 2 个活跃信号：
- **宽松预期增强**（US02Y↓ + US10Y↓ + DFII10↓）：利率全面下行
- **信用风险上升**（BAMLH0A0HYM2↑ + MOVE↑）：利差走阔、波动上升
